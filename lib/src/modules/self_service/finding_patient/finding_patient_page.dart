import 'package:flutter/material.dart';

class FindingPatientPage extends StatelessWidget {

  const FindingPatientPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Fiding Patient'),),
           body: Container(),
       );
  }
}